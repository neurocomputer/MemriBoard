{
    "name": "example_for_cycle",
    "mode": "algorithm",
    "params": {
        "wl": 0,
        "bl": 0
    },
    "code": "\"\"\"\nAn example of the algorithm with 'for' cycle and ticket parameter changing\nПример реализации алгоритма с циклом 'for' и измененем параметров тикетов\n\nThe algorithm implements double sweep: primary sweep is done in the 'iv_curve' \nticket, secondary sweep is done via 'for' cycle.\nPulse width sweep is from 100 to 1000 us, 10 points.\n\nАлгоритм реализует двойную развертку: первичная развертка по напряжению идет\nв тикете iv_curve, вторичная развертка по ширине импульса реализуется циклом for.\nРазвертка по ширине импульса от 100 до 1000 мкс, 10 точек.\n\"\"\"\nimport numpy as np\n\n# Helper function that changes pulse width in the ticket\n# Вспомогательная функция изменения ширины импульса в тикете\ndef change_pulse_width(ticket, width_us):  # width is in us | ширина импульса в мкс\n    if int(width_us) < 1000:  # Changing usec parameter | пишем в микросекундах\n        ticket['params']['t_dir_usec_inc'] = int(width_us)\n        ticket['params']['t_dir_usec_dec'] = int(width_us)\n        ticket['params']['t_rev_usec_inc'] = int(width_us)\n        ticket['params']['t_rev_usec_dec'] = int(width_us)\n        ticket['params']['t_dir_msec_inc'] = 0\n        ticket['params']['t_dir_msec_dec'] = 0\n        ticket['params']['t_rev_msec_inc'] = 0\n        ticket['params']['t_rev_msec_dec'] = 0\n    else:  # Changing msec parameter | пишем в миллисекундах\n        ticket['params']['t_dir_usec_inc'] = 0\n        ticket['params']['t_dir_usec_dec'] = 0\n        ticket['params']['t_rev_usec_inc'] = 0\n        ticket['params']['t_rev_usec_dec'] = 0\n        ticket['params']['t_dir_msec_inc'] = int(width_us / 1000)\n        ticket['params']['t_dir_msec_dec'] = int(width_us / 1000)\n        ticket['params']['t_rev_msec_inc'] = int(width_us / 1000)\n        ticket['params']['t_rev_msec_dec'] = int(width_us / 1000)\n    return ticket\n\n\n# Main algorithm function | Функция алгоритма\ndef algorithm():\n    # Creating pulse width array | Создаем массив ширины импульса\n    width_array = np.linspace(100, 1000, 10, dtype=int)\n    # Getting ticket as a dict | Получаем тикет как dict для редактирования\n    base_ticket = get_ticket_dict('iv-curve')\n    # Creating for cycle | Делаем цикл for\n    for width in width_array:\n        # Editing ticket | Редактируем тикет\n        ticket = change_pulse_width(base_ticket, width)\n        # Executing ticket | Выполняем тикет\n        send_ticket_dict(ticket)\n    ",
    "tickets": {
        "iv-curve": {
            "name": "iv-curve",
            "mode": "std",
            "params": {
                "v_dir_strt_inc": 0,
                "v_dir_stop_inc": 1392,
                "v_dir_step_inc": 41,
                "t_dir_msec_inc": 0,
                "t_dir_usec_inc": 100,
                "dir_inc_countr": 1,
                "v_dir_strt_dec": 1392,
                "v_dir_stop_dec": 0,
                "v_dir_step_dec": 41,
                "t_dir_msec_dec": 0,
                "t_dir_usec_dec": 100,
                "dir_dec_countr": 1,
                "v_rev_strt_inc": 0,
                "v_rev_stop_inc": 1392,
                "v_rev_step_inc": 41,
                "t_rev_msec_inc": 0,
                "t_rev_usec_inc": 100,
                "rev_inc_countr": 1,
                "v_rev_strt_dec": 1392,
                "v_rev_stop_dec": 0,
                "v_rev_step_dec": 41,
                "t_rev_msec_dec": 0,
                "t_rev_usec_dec": 100,
                "rev_dec_countr": 1,
                "count": 1,
                "reverse": 0,
                "id": 0,
                "wl": 0,
                "bl": 0
            },
            "terminate": {
                "type": "pass",
                "value": 0
            }
        }
    },
    "executed_tickets": []
}